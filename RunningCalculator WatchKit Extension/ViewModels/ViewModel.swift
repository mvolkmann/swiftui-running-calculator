import SwiftUI

final class ViewModel: ObservableObject {
    // TODO: Change these default values?
    //@Published var distanceKey = "Marathon"
    //@Published var distanceUnit = "mile"
    //@Published var paceSeconds = 6 * 60 + 45
    //@Published var totalSeconds = (2 * 60 + 57) * 60 + 11
}
